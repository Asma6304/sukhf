'use client'

import { useEffect, useMemo, useState } from 'react'
import {
  Bell, BriefcaseBusiness, CalendarDays, Check, ChevronDown, ChevronLeft, ChevronRight, ClipboardList,
  Compass, FileText, Headphones, House, Lightbulb, MapPin, Menu, Monitor, Share2, Rocket, Search, Settings,
  ShieldCheck, Sparkles, Trophy, UserRound, UsersRound, GraduationCap, Star, Link2, Heart, Bot, Cpu, X, Mail, Phone, MapPinned, Pencil, Globe2,
  Plus, ExternalLink, Clock3, CheckCircle2, Flag, CircleAlert, LockKeyhole, Camera, Eye, SlidersHorizontal, Send, MessageCircle
} from 'lucide-react'

type IconType = typeof House
type View = 'dashboard' | 'profile' | 'opportunities' | 'requests' | 'applications' | 'projects' | 'mentor' | 'events' | 'notifications' | 'settings'

const navItems: { label: string; path: View; icon: IconType }[] = [
  { label: 'Dashboard', path: 'dashboard', icon: House }, { label: 'My Profile', path: 'profile', icon: UserRound },
  { label: 'Explore Opportunities', path: 'opportunities', icon: Rocket }, { label: 'My Requests', path: 'requests', icon: ClipboardList },
  { label: 'My Applications', path: 'applications', icon: FileText }, { label: 'My Projects', path: 'projects', icon: BriefcaseBusiness },
  { label: 'My Mentor', path: 'mentor', icon: UsersRound }, { label: 'Events & Activities', path: 'events', icon: CalendarDays },
  { label: 'Notifications', path: 'notifications', icon: Bell }, { label: 'Settings', path: 'settings', icon: Settings },
]
const categories = [['Innovation', Lightbulb, 'blue'], ['Startup', Rocket, 'green'], ['Major Project', Sparkles, 'purple'], ['Internship', BriefcaseBusiness, 'orange'], ['Research', Search, 'blue'], ['Mentorship', UsersRound, 'pink'], ['IPR', ShieldCheck, 'green'], ['Drone', Sparkles, 'purple'], ['AI / IoT', Sparkles, 'yellow'], ['Competition', Trophy, 'pink']] as const
const opportunityTemplates = [
  ['Competition', Trophy, 'yellow', ['AI/ML', 'Social Impact'], ['AI for Social Impact Hackathon', 'Climate Tech Challenge', 'Campus Impact Cup', 'Future Builders Contest', 'Digital Inclusion Challenge', 'Sustainable Ideas League']],
  ['Internship', BriefcaseBusiness, 'blue', ['IoT', 'Embedded C', 'ESP32'], ['Research Internship – IoT', 'Software Engineering Internship', 'Product Design Internship', 'Data Science Internship', 'Social Impact Internship', 'Robotics Lab Internship']],
  ['Innovation', Lightbulb, 'green', ['Prototyping', 'Ideation'], ['Smart City Idea Challenge', 'Student Innovation Challenge', 'Green Future Challenge', 'Healthcare Innovation Sprint', 'Education Ideas Lab', 'Community Solutions Forum']],
  ['Mentorship', UsersRound, 'pink', ['Career Guidance', 'Research'], ['Mentorship Program for Students', 'Women in Technology Mentorship', 'Founder Guidance Circle', 'Research Career Mentoring', 'Design Portfolio Mentorship', 'Leadership Growth Program']],
  ['Startup', Rocket, 'green', ['Startup', 'Business Model'], ['GreenTech Startup Accelerator', 'SUKHF Pre-Incubation Cohort', 'Social Venture Launchpad', 'Student Founder Program', 'Impact Startup Studio', 'Youth Enterprise Sprint']],
  ['Research', ShieldCheck, 'blue', ['Deep Tech', 'Research'], ['Women in Deep Tech Fellowship', 'Applied AI Research Grant', 'IoT Research Fellowship', 'Climate Research Program', 'Open Science Fellowship', 'Humanities Research Lab']],
  ['Major Project', BriefcaseBusiness, 'purple', ['Python', 'LLMs', 'Testing'], ['LLM-Based Test Generation Platform', 'Smart Campus Major Project', 'Open Source Engineering Project', 'Digital Health Platform', 'AI Research Capstone', 'Sustainable Systems Project']],
  ['IPR', ShieldCheck, 'green', ['IPR', 'Documentation'], ['Patent Filing Support Program', 'IPR Awareness Workshop', 'Student Patent Clinic', 'Copyright Guidance Cell', 'Prior Art Research Camp', 'Innovation Protection Desk']],
  ['Drone', Bot, 'purple', ['UAV', 'Robotics'], ['Campus Drone Innovation Sprint', 'Drone Technology Workshop', 'UAV Mapping Challenge', 'Autonomous Flight Lab', 'Aerial Robotics Program', 'Drone Safety Fellowship']],
  ['AI / IoT', Cpu, 'yellow', ['IoT', 'Data Science', 'MATLAB'], ['Smart Agriculture Sensor Network', 'AI for Good Lab', 'Connected Devices Challenge', 'Machine Learning Bootcamp', 'Edge AI Research Sprint', 'Intelligent Systems Forum']],
] as const
const opportunities = opportunityTemplates.flatMap(([type, icon, tone, tags, titles], categoryIndex) => titles.map((title, index) => ({ title, org: index % 2 ? 'SUKHF' : 'SUKHF Innovation Cell', type, place: index % 3 === 0 ? 'Online' : index % 3 === 1 ? 'Hyderabad' : 'Hybrid', date: `Oct ${15 + ((categoryIndex * 3 + index) % 15)}, 2026`, icon, tone, tags })))
const applications = [
  ['AI for Social Impact Hackathon', 'Competition', 'Selected', 'Oct 15, 2026'], ['Research Internship – IoT', 'Internship', 'Under Review', 'Oct 20, 2026'], ['Smart City Idea Challenge', 'Innovation', 'Shortlisted', 'Nov 05, 2026'], ['GreenTech Startup Program', 'Startup', 'Rejected', 'Sep 28, 2026']
]
const requests = [['Need a Mentor for AI Project', 'Mentorship', 'Under Review', 'Sep 20, 2026'], ['Request for Internship Guidance', 'Guidance', 'Approved', 'Sep 10, 2026'], ['Request for IPR Support', 'IPR', 'Pending', 'Sep 05, 2026']]
const projects = [['AI-Based Study Planner', 'Major Project', 'Active', 'Aug 10, 2026', 70, Sparkles, 'purple'], ['Sustainable Campus Initiative', 'Research', 'Done', 'Apr 15, 2026', 100, CheckCircle2, 'green'], ['Drone Mapping for Rural Development', 'Innovation', 'On Hold', 'Jun 01, 2026', 40, BriefcaseBusiness, 'orange'], ['Smart Attendance System (IoT)', 'Internship', 'Active', 'Sep 05, 2026', 55, Bot, 'pink']]
const notifications = [['Application Shortlisted', 'Your application for AI for Social Impact Hackathon has been shortlisted for the next round.', '10:30 AM', 'success'], ['New Event Added', 'A new workshop "Drone Technology for Beginners" has been added. Check out the details now!', 'Yesterday', 'info'], ['Mentor Meeting Scheduled', 'Dr. Arjun Mehta has scheduled a mentoring session with you on Oct 10, 2026 at 4:00 PM.', 'Sep 28, 2026', 'warning'], ['New Opportunity Matching Your Profile', 'A research internship opportunity at TechSphere Labs matches your interests.', 'Sep 25, 2026', 'info'], ['Registration Confirmed', 'Your registration for the Sustainable Innovation Seminar is confirmed.', 'Sep 22, 2026', 'success'], ['New Message from Mentor', 'You have a new message from Dr. Arjun Mehta.', 'Sep 20, 2026', 'info'], ['Profile Update Reminder', 'Complete your profile to get better opportunity recommendations.', 'Sep 18, 2026', 'success'], ['Deadline Reminder', 'The deadline for SUKHF Innovation Challenge 2026 is in 3 days.', 'Sep 15, 2026', 'warning']]

function pathToView() { const path = window.location.pathname.replace(/^\//, '') as View; return navItems.some((item) => item.path === path) ? path : 'dashboard' }
function navigate(view: View) { window.history.pushState({}, '', view === 'dashboard' ? '/' : `/${view}`); window.dispatchEvent(new PopStateEvent('popstate')) }

export default function Page() {
  const [view, setView] = useState<View>('dashboard'); const [query, setQuery] = useState(''); const [menuOpen, setMenuOpen] = useState(false); const [profileMenu, setProfileMenu] = useState(false)
  const [applied, setApplied] = useState<string[]>([]); const [category, setCategory] = useState('All'); const [unread, setUnread] = useState(3); const [editing, setEditing] = useState(false); const [allOpportunities, setAllOpportunities] = useState(opportunities)
  useEffect(() => { const sync = () => setView(pathToView()); setView(pathToView()); window.addEventListener('popstate', sync); return () => window.removeEventListener('popstate', sync) }, []); useEffect(() => { setAllOpportunities([...opportunities].sort(() => Math.random() - 0.5)) }, [])
  const go = (next: View) => { navigate(next); setView(next); setMenuOpen(false) }
  const filtered = useMemo(() => allOpportunities.filter((item) => `${item.title} ${item.org} ${item.type}`.toLowerCase().includes(query.toLowerCase()) && (category === 'All' || item.type === category)), [allOpportunities, query, category])
  return <div className="dashboard-shell"><Sidebar active={view} go={go} menuOpen={menuOpen} close={() => setMenuOpen(false)} unread={unread} /><main className="main-area"><header className="topbar"><button className="menu-button" onClick={() => setMenuOpen(true)} aria-label="Open menu"><Menu /></button><div className="search-wrap"><Search /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search opportunities, events, mentors..." aria-label="Search" /></div><div className="top-actions"><button className="icon-button" aria-label="Open notifications" onClick={() => go('notifications')}><Bell /><i /></button><div className="profile-menu"><div className="avatar">A</div><div className="profile-copy"><strong>Asma</strong><span>Student</span></div><button aria-label="Open profile menu" onClick={() => setProfileMenu(!profileMenu)}><ChevronDown /></button>{profileMenu && <div className="profile-dropdown"><button onClick={() => go('profile')}>View profile</button><button onClick={() => setProfileMenu(false)}>Sign out</button></div>}</div></div></header><div className={`content ${view === 'dashboard' ? 'dashboard-page' : ''} ${view === 'opportunities' ? 'opportunities-page' : ''}`}>{view === 'dashboard' && <Dashboard go={go} category={category} setCategory={setCategory} filtered={filtered} applied={applied} setApplied={setApplied} query={query} />}{view === 'profile' && <Profile editing={editing} setEditing={setEditing} />}{view === 'opportunities' && <Opportunities filtered={filtered} category={category} setCategory={setCategory} applied={applied} setApplied={setApplied} query={query} />}{view === 'requests' && <RequestForm />}{view === 'applications' && <ApplicationsPage go={go} />}{view === 'projects' && <Projects go={go} />}{view === 'mentor' && <MentorDashboard />}{view === 'events' && <Events />}{view === 'notifications' && <Notifications unread={unread} setUnread={setUnread} />}{view === 'settings' && <SettingsView />}</div></main></div>
}

function Sidebar({ active, go, menuOpen, close, unread }: { active: View; go: (v: View) => void; menuOpen: boolean; close: () => void; unread: number }) { return <aside className={`sidebar ${menuOpen ? 'sidebar-open' : ''}`}><div className="brand"><div className="brand-mark"><img src="/sukhf-logo.jpeg" alt="" /></div><div><strong>SUKHF</strong><small>Innovation & Opportunity<br />Management Platform</small></div><button className="mobile-close" onClick={close} aria-label="Close menu"><X /></button></div><nav className="nav-list" aria-label="Primary navigation">{navItems.map(({ label, path, icon: Icon }) => <button key={path} className={`nav-item ${active === path ? 'active' : ''}`} onClick={() => go(path)}><Icon /><span>{label}</span>{path === 'notifications' && <b className="notif-count">{unread}</b>}</button>)}</nav><div className="sidebar-quote"><p>Learn<br />Build<br />Collaborate<br />Create Impact</p><span>—</span><Sparkles className="quote-leaves" aria-hidden="true" /></div><footer>© 2026 SUKHF<br />All rights reserved.</footer></aside> }
function PageIntro({ eyebrow = 'STUDENT PORTAL', title, subtitle, action }: { eyebrow?: string; title: string; subtitle: string; action?: React.ReactNode }) { return <div className="page-intro"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="subtitle">{subtitle}</p></div>{action}</div> }
function Panel({ title, children, action }: { title: string; children: React.ReactNode; action?: React.ReactNode }) { return <section className="panel"><div className="section-heading"><h2>{title}</h2>{action}</div>{children}</section> }
function Dashboard({ go, category, setCategory, filtered, applied, setApplied, query }: any) { return <><section className="welcome-row"><div><p className="eyebrow">STUDENT PORTAL</p><h1>Welcome back, Asma!</h1><p className="subtitle">Explore opportunities, collaborate with mentors and build your journey with SUKHF.</p></div><div className="quote-card"><span>“</span><em>Opportunities don’t happen,<br />you create them.</em><small>— SUKHF</small></div></section><section className="stats-grid">{[['3', 'My Requests', 'requests', ClipboardList, 'blue'], ['5', 'My Applications', 'applications', Rocket, 'green'], ['2', 'My Projects', 'projects', Sparkles, 'purple'], ['1', 'My Mentor', 'mentor', UsersRound, 'orange']].map(([n, label, path, Icon, tone]: any) => <button className={`stat-card ${tone}`} key={label} onClick={() => go(path)}><span className="stat-icon"><Icon /></span><span><strong>{n}</strong><small>{label}</small></span><ChevronRight /></button>)}<div className="completion-card"><div className="progress-ring"><span>80%</span></div><div><strong>Profile Completion</strong><p>Complete your profile to get<br />better recommendations.</p><button onClick={() => go('profile')}>Complete Profile</button></div></div></section><Panel title="Explore Opportunity Categories" action={<button onClick={() => go('opportunities')}>View All</button>}><div className="category-grid">{categories.map(([label, Icon, tone]: any) => <button className={`category-card ${tone} ${category === label ? 'selected' : ''}`} key={label} onClick={() => { setCategory(category === label ? 'All' : label); go('opportunities') }}><Icon /><span>{label}</span></button>)}</div></Panel><div className="two-column"><OpportunityPanel applied={applied} setApplied={setApplied} go={go} /><Panel title="My SUKHF Journey" action={<button onClick={() => go('applications')}>View Details</button>}><Journey /></Panel></div><div className="bottom-grid"><Panel title="Upcoming Events" action={<button onClick={() => go('events')}>View All</button>}><EventRows compact /></Panel><Panel title="Latest Announcements" action={<button onClick={() => go('notifications')}>View All</button>}><AnnouncementRows /></Panel><Panel title="Need Help?"><div className="help-content"><span><Headphones /></span><div><p>Have questions or need guidance?<br />Reach out to our support team.</p><button onClick={() => go('mentor')}>Contact Support</button></div></div></Panel></div></> }
function OpportunityPanel({ applied, setApplied, go }: any) { const recommendations = opportunities.slice(0, 3); return <Panel title="Recommended Opportunities" action={<button onClick={() => go('opportunities')}>View All</button>}><div className="opportunity-list dashboard-opportunity-list">{recommendations.map((item: any) => <OpportunityRow key={item.title} item={item} applied={applied} setApplied={setApplied} compact />)}</div></Panel> }
function Highlight({ text, query }: { text: string; query: string }) { if (!query.trim()) return <>{text}</>; const parts = text.split(new RegExp(`(${query.replace(/[.*+?^${}()|[\\]\\]/g, '\\$&')})`, 'gi')); return <>{parts.map((part, index) => part.toLowerCase() === query.trim().toLowerCase() ? <mark key={`${part}-${index}`}>{part}</mark> : part)}</> }
function OpportunityRow({ item, applied, setApplied, query = '', compact = false }: any) { const Icon = item.icon; const [favorite, setFavorite] = useState(false); return <article className={`${compact ? 'dashboard-opportunity-row' : 'opportunity-card'} ${item.tone}`}><div className="opportunity-card-top"><span className="opportunity-icon"><Icon /></span></div><span className="opportunity-type"><Highlight text={item.type} query={query} /></span><div className="opportunity-info"><strong><Highlight text={item.title} query={query} /></strong><span><Highlight text={item.org} query={query} /></span><div><small><MapPin /><Highlight text={item.place} query={query} /></small><small><CalendarDays />Applications close {item.date}</small></div></div><div className="opportunity-tags">{(item.tags || []).map((tag: string) => <span key={tag}>{tag}</span>)}</div><div className="opportunity-card-footer"><button className="apply-button" onClick={() => setApplied((current: string[]) => current.includes(item.title) ? current : [...current, item.title])}>{applied.includes(item.title) ? 'Applied ✓' : 'Apply'}</button><span>Closes in <b>{item.type === 'Competition' ? '40' : item.type === 'Internship' ? '45' : '57'} days</b></span></div></article> }
function Opportunities({ filtered, category, setCategory, applied, setApplied, query }: any) { const [filtersOpen, setFiltersOpen] = useState(false); const [search, setSearch] = useState(''); const [location, setLocation] = useState('All Locations'); const [domain, setDomain] = useState('All Domains'); const [eligibility, setEligibility] = useState('All Eligibility'); const [activeFilters, setActiveFilters] = useState({ search: '', location: 'All Locations', domain: 'All Domains', eligibility: 'All Eligibility' }); const displayed = filtered.filter((item: any) => { const text = `${item.title} ${item.org} ${item.type} ${(item.tags || []).join(' ')}`.toLowerCase(); const searchMatch = text.includes(activeFilters.search.toLowerCase()); const locationMatch = activeFilters.location === 'All Locations' || item.place === activeFilters.location; const domainMatch = activeFilters.domain === 'All Domains' || (item.tags || []).some((tag: string) => tag.toLowerCase().includes(activeFilters.domain.toLowerCase())); const eligibilityMatch = activeFilters.eligibility === 'All Eligibility' || activeFilters.eligibility === 'Students'; return searchMatch && locationMatch && domainMatch && eligibilityMatch; }); const applyFilters = () => { setActiveFilters({ search, location, domain, eligibility }); setFiltersOpen(true); }; return <><PageIntro title="Explore Opportunities" subtitle="Find the right opportunities to learn, build and grow." /><Panel title="All Opportunities" action={<span className="result-count">{displayed.length} opportunities</span>}><div className="opportunity-search-row"><div className="opportunity-search"><Search /><input value={search} onChange={(event) => setSearch(event.target.value)} onKeyDown={(event) => { if (event.key === 'Enter') applyFilters(); }} placeholder="Search by title, domain or skill (e.g. Python, IoT, Drone)" /></div><label className="filter-select"><MapPin /><select value={location} onChange={(event) => setLocation(event.target.value)}><option>All Locations</option><option>Online</option><option>Hyderabad</option><option>Hybrid</option></select><ChevronDown /></label><label className="filter-select"><BriefcaseBusiness /><select value={domain} onChange={(event) => setDomain(event.target.value)}><option>All Domains</option><option>AI/ML</option><option>Social Impact</option><option>Research</option></select><ChevronDown /></label><label className="filter-select"><UserRound /><select value={eligibility} onChange={(event) => setEligibility(event.target.value)}><option>All Eligibility</option><option>Students</option><option>All applicants</option></select><ChevronDown /></label><button className="primary-button filter-apply-button" onClick={applyFilters}>Apply Filters</button></div><div className="filter-row category-filters">{[['All Categories', Sparkles], ...categories].map(([item, Icon]: any) => <button className={category === item || (item === 'All Categories' && category === 'All') ? 'filter-chip active' : 'filter-chip'} key={item} onClick={() => setCategory(item === 'All Categories' ? 'All' : item)}><Icon />{item}</button>)}</div><div className="opportunity-list opportunity-grid large-list">{displayed.map((item: any) => <OpportunityRow key={item.title} item={item} applied={applied} setApplied={setApplied} query={query} />)}</div></Panel></> }
function Profile({ editing, setEditing }: any) { const [saved, setSaved] = useState(false); const [tab, setTab] = useState('Personal Info'); const [fields, setFields] = useState({ name: 'Asma', dob: '12 Mar 2005', gender: 'Female', email: 'asma@example.com', phone: '+91 98765 43210', location: 'Hyderabad, India', bio: 'Passionate about technology, innovation and social impact. Looking forward to learning and contributing through SUKHF opportunities.', university: 'Osmania University', program: 'B.A. (Bachelor of Arts)', year: '3rd Year', cgpa: '8.7 / 10', graduation: 'May 2028', studentId: 'OU2023BA0156' }); const tabs = ['Personal Info', 'Academic Details', 'Skills & Interests', 'Documents', 'Social Links']; const update = (key: string, value: string) => setFields((current) => ({ ...current, [key]: value })); return <><div className="profile-heading-row"><div><p className="breadcrumb">Dashboard <ChevronRight /> My Profile</p><PageIntro title="My Profile" subtitle="Manage your personal information, academic details and interests." /></div></div><div className="profile-hero"><div className="large-avatar">A<button className="avatar-camera" onClick={() => window.alert('Profile photo picker opened')} aria-label="Change profile photo"><Camera /></button></div><div className="profile-hero-copy"><h2>Asma</h2><p>Student</p><span>Osmania University, Hyderabad</span><div className="profile-contact"><span><Mail /> asma@example.com</span><span><Phone /> +91 98765 43210</span><span><MapPinned /> Hyderabad, Telangana</span></div></div><button className="primary-button profile-edit-button" onClick={() => { setEditing(!editing); setSaved(false) }}><Pencil /> {editing ? 'Cancel Editing' : 'Edit Profile'}</button><div className="hero-completion"><h2>Profile Completion</h2><div className="hero-completion-body"><div className="progress-ring"><span>80%</span></div><p>Complete your profile to get better recommendations.</p></div></div></div><div className="profile-layout"><Panel title="Profile overview"><div className="profile-card"><div className="large-avatar">N</div><h2>Asma</h2><p>Student</p><span><Mail /> asma@example.com</span><span><Phone /> +91 98765 43210</span><span><MapPinned /> Hyderabad, India</span><div className="mini-progress"><span style={{ width: '80%' }} /></div><small>Profile Completion <b>80%</b></small></div></Panel><Panel title="Profile Completion"><div className="completion-checklist"><div className="progress-ring"><span>80%</span></div>{['Personal Information', 'Academic Details', 'Skills & Interests', 'Documents', 'Social Links', 'Preferences'].map((item, index) => <button key={item} onClick={() => index < 4 ? setTab(item) : setSaved(true)}><CheckCircle2 className={index === 4 ? 'incomplete' : ''} /> {item}</button>)}</div></Panel><Panel title="Quick Links"><div className="quick-links"><button onClick={() => setTab('Documents')}><FileText /><span><strong>Upload Documents</strong><small>Add certificates and resumes</small></span><ChevronRight /></button><button onClick={() => setTab('Skills & Interests')}><Sparkles /><span><strong>Manage Skills</strong><small>Edit your skills and interests</small></span><ChevronRight /></button><button onClick={() => setTab('Social Links')}><ExternalLink /><span><strong>Link Social Accounts</strong><small>Connect LinkedIn, GitHub, etc.</small></span><ChevronRight /></button><button onClick={() => setTab('Preferences')}><Settings /><span><strong>Profile Visibility</strong><small>Control who can see your profile</small></span><ChevronRight /></button></div></Panel><Panel title={tab}><div className="tabs profile-tabs">{tabs.map((item) => <button key={item} className={tab === item ? 'active' : ''} onClick={() => setTab(item)}>{item === 'Personal Info' ? <UserRound /> : item === 'Academic Details' ? <GraduationCap /> : item === 'Skills & Interests' ? <Star /> : item === 'Documents' ? <FileText /> : item === 'Social Links' ? <Link2 /> : <Settings />}{item}</button>)}</div>{tab === 'Personal Info' && <><div className="form-grid">{[['Full Name', 'Asma'], ['Date of Birth', '12 Mar 2005'], ['Gender', 'Female'], ['Email', 'asma@example.com'], ['Phone', '+91 98765 43210'], ['Location', 'Hyderabad, India']].map(([label, value]) => <label key={label}>{label}<input disabled={!editing} value={fields[label === 'Full Name' ? 'name' : label === 'Date of Birth' ? 'dob' : label === 'Gender' ? 'gender' : label === 'Email' ? 'email' : label === 'Phone' ? 'phone' : 'location']} onChange={(event) => update(label === 'Full Name' ? 'name' : label === 'Date of Birth' ? 'dob' : label === 'Gender' ? 'gender' : label === 'Email' ? 'email' : label === 'Phone' ? 'phone' : 'location', event.target.value)} /></label>)}</div><label className="full-field">Bio<textarea disabled={!editing} value={fields.bio} onChange={(event) => update('bio', event.target.value)} /></label></>}{tab === 'Academic Details' && <div className="academic-form"><label>Current Institution<input disabled={!editing} value={fields.university} onChange={(event) => update('university', event.target.value)} /></label><label>Course / Program<input disabled={!editing} value={fields.program} onChange={(event) => update('program', event.target.value)} /></label><label>Year of Study<select disabled={!editing} value={fields.year} onChange={(event) => update('year', event.target.value)}><option>1st Year</option><option>2nd Year</option><option>3rd Year</option><option>Final Year</option></select></label><label>CGPA<input disabled={!editing} value={fields.cgpa} onChange={(event) => update('cgpa', event.target.value)} /></label><label>Graduation Date<input disabled={!editing} value={fields.graduation} onChange={(event) => update('graduation', event.target.value)} /></label><label>Roll Number / Student ID<input disabled={!editing} value={fields.studentId} onChange={(event) => update('studentId', event.target.value)} /></label></div>}{tab === 'Skills & Interests' && <div className="skills-detail"><p>Build a profile that helps us recommend the right opportunities.</p><div className="interest-row">{['AI / ML', 'Web Development', 'Social Impact', 'Research', 'Startups', 'Python', 'Product Design', 'Public Speaking'].map((item) => <span key={item}>{item}</span>)}<button onClick={() => setSaved(true)}><Plus /> Add Skill</button></div></div>}{tab === 'Documents' && <div className="documents-detail">{['Resume_Asma.pdf', 'Academic_Transcript_2026.pdf', 'Portfolio_Link.txt'].map((item) => <div className="document-row" key={item}><FileText /><div><strong>{item}</strong><small>Uploaded recently · PDF document</small></div><button className="ghost-button" onClick={() => setSaved(true)}>View <ExternalLink /></button></div>)}<button className="secondary-button" onClick={() => setSaved(true)}><Plus /> Upload Document</button></div>}<div className="interest-row"><strong>Interests</strong>{['AI / ML', 'Web Development', 'Social Impact', 'Research', 'Startups'].map((item) => <span key={item}>{item}</span>)}<button onClick={() => setTab('Skills & Interests')}><Plus /> Add</button></div>{editing && <button className="primary-button save-button" onClick={() => { setSaved(true); setEditing(false) }}>{saved ? 'Saved' : 'Save Changes'}</button>}{saved && <small className="inline-success">Changes saved successfully.</small>}</Panel></div></> }
function Info({ label, value }: { label: string; value: string }) { return <div className="info-card"><small>{label}</small><strong>{value}</strong></div> }
function Modal({ title, children, onClose }: { title: string; children: React.ReactNode; onClose: () => void }) { return <div className="modal-backdrop"><div className="modal-card" role="dialog" aria-modal="true" aria-label={title}><div className="modal-heading"><h2>{title}</h2><button className="icon-button" onClick={onClose} aria-label="Close"><X /></button></div>{children}</div></div> }
function RequestForm() { const [requirement, setRequirement] = useState(''); const [submitted, setSubmitted] = useState(false); const types = [['Internship', BriefcaseBusiness, 'blue'], ['Major Project', BriefcaseBusiness, 'purple'], ['Research', Search, 'blue'], ['Startup Support', Rocket, 'green'], ['Mentorship', UsersRound, 'pink'], ['IPR / Patent', ShieldCheck, 'mint'], ['Training', GraduationCap, 'yellow']] as const; return <div className="request-page"><p className="breadcrumb">Dashboard <ChevronRight /> My Requests <ChevronRight /> <strong>New Request</strong></p><PageIntro title="Raise a New Request" subtitle="Tell us what you’re looking for — we'll match it to suitable opportunities and mentors." /><div className="request-steps"><span className={requirement ? 'complete' : 'current'}>{requirement ? <Check /> : <b>1</b>} Requirement Type</span><i /><span className={submitted ? 'complete' : requirement ? 'current' : ''}>{submitted ? <Check /> : <b>2</b>} Requirement Details</span><i /><span className={submitted ? 'current' : ''}><b>3</b> Preferences & Documents</span><i /><span><b>4</b> Review & Submit</span></div><div className="request-layout"><div className="request-main panel"><h2>What are you looking for?</h2><p className="request-help">Select the type of support that best matches your requirement.</p><div className="request-types">{types.map(([label, Icon, tone]) => <button key={label} className={`request-type ${tone} ${requirement === label ? 'selected' : ''}`} onClick={() => setRequirement(label)}><Icon />{label}</button>)}</div><h2>Requirement Details</h2><p className="request-help">Describe your project so coordinators can screen and match it accurately.</p><div className="request-form-grid"><label>Project / Requirement Title *<input required placeholder="Enter your project or requirement title" /></label><label>Domain / Interest Area *<input required placeholder="Enter domain or interest area" /></label><label>Institution *<input required placeholder="Enter your institution" /></label><label>Duration<select ><option value="">Select duration</option><option>3 months</option><option>6 months</option><option>1 year</option></select></label><label>Preferred Mode<select ><option value="">Select preferred mode</option><option>Remote / Independent</option><option>Hybrid</option><option>On campus</option></select></label><label className="request-full">Requirement Description *<textarea required placeholder="Describe your project or requirement in detail" /><button className="primary-button request-apply-button" type="button" onClick={() => setSubmitted(true)}><Check /> Apply</button></label></div><label className="request-label">Key Skills</label><div className="skill-chips"><span>Python ×</span><span>LLMs / Gemini API ×</span><span>pytest ×</span><span>Mutation Testing ×</span><button>+ Add skill</button></div><h2>Supporting Documents</h2><p className="request-help">Attach anything that helps a coordinator evaluate the request.</p><div className="upload-box"><FileText /><strong>Drag files here or click to upload</strong><small>PDF, DOCX up to 10MB</small></div><div className="uploaded-file"><FileText /> TestGen_Abstract_and_Literature_Review.pdf <small>1.2 MB</small></div><div className="uploaded-file"><FileText /> PROJECT_CHECKLIST.md <small>14 KB</small></div><div className="request-actions"><button className="secondary-button">← Back</button><button className="primary-button" onClick={() => setSubmitted(true)}>{submitted ? 'Request Saved ✓' : 'Continue to Preferences →'}</button></div></div><aside className="request-aside"><div className="draft-card"><small>DRAFT REQUEST ID</small><strong>SUKHF-REQ-2026-0412</strong><p>A unique Request ID is generated once you submit. Coordinators use this to track screening and mapping.</p></div><div className="summary-card"><h2><ClipboardList /> Request Summary</h2><p>Requester <b>Asma</b></p><p>Type <b>{requirement}</b></p><p>Domain <b>AI / Software Engineering</b></p><p>Duration <b>3 months</b></p><p>Mode <b>Remote</b></p><p>Status <b>Draft</b></p></div><div className="tip-card"><Lightbulb /><p><strong>Tip</strong><br />A clear, specific description — including your novelty/gap and mentor expertise you need — helps coordinators map you faster.</p></div></aside></div></div> }
function ApplicationsPage({ go }: { go: (view: View) => void }) { const [tab, setTab] = useState('All Applications'); const [statusFilter, setStatusFilter] = useState('All'); const [openMenu, setOpenMenu] = useState<string | null>(null); const applicationRows = [['AI-Powered Campus Navigator', 'Innovation', 'Shortlisted', 'AI / Machine Learning', '12 May 2026', Sparkles, 'purple'], ['Autonomous Drone for Crop Monitoring', 'Major Project', 'In Review', 'Drone / IoT', '10 May 2026', Bot, 'purple'], ['Software Engineering Intern', 'Internship', 'Selected', 'Software Engineering', '08 May 2026', BriefcaseBusiness, 'blue'], ['AI Research Assistantship', 'Research', 'In Review', 'AI / NLP', '05 May 2026', Search, 'pink'], ['IPR & Patent Filing Support', 'IPR / Patent', 'Not Selected', 'IPR / Legal', '02 May 2026', ShieldCheck, 'yellow']]; const visible = applicationRows.filter((row) => (tab === 'All Applications' || row[2] === tab.replace(' (2)', '')) && (statusFilter === 'All' || row[2] === statusFilter)); return <div className="applications-page"><PageIntro title="My Applications" subtitle="Track and manage all the opportunities you've applied for." /><div className="application-stat-grid">{[['8', 'Total Applications', 'All time', FileText, 'blue'], ['2', 'In Review', 'Currently under review', Clock3, 'orange'], ['2', 'Shortlisted', 'Move to next round', Star, 'purple'], ['2', 'Selected', "You've been selected", CheckCircle2, 'green'], ['2', 'Not Selected', 'Not selected', CircleAlert, 'pink']].map(([count, label, note, Icon, tone]: any) => <div className={`application-stat ${tone}`} key={label}><span><Icon /></span><div><strong>{count}</strong><b className="application-stat-label">{label}</b><small>{note}</small></div></div>)}</div><div className="applications-layout"><div className="application-list-panel panel"><div className="application-tabs">{['All Applications', 'In Review (2)', 'Shortlisted (2)', 'Selected (2)', 'Not Selected (2)'].map((item) => <button key={item} className={tab === item ? 'active' : ''} onClick={() => setTab(item)}>{item}</button>)}<div className="application-controls"><label className="filter-application"><SlidersHorizontal /> Filters<select aria-label="Filter applications" value={statusFilter} onChange={(event) => setStatusFilter(event.target.value)}><option>All</option><option>In Review</option><option>Shortlisted</option><option>Selected</option><option>Not Selected</option></select></label></div></div><div className="application-rows">{visible.map(([title, type, stage, domain, date, Icon, tone]: any, index) => <article className="application-row" key={title}><span className={`application-icon ${tone}`}><Icon /></span><div className="application-main"><strong>{title}</strong><span className={`application-tag ${tone}`}>{type}</span><small>Domain: <b>{domain}</b></small><small>Applied on: <b>{date}</b> <i>•</i> Ref ID: APP-2026-000{index + 1}</small></div><div className="application-stage"><small>Stage</small><Status text={stage} /></div><div className="application-update"><small>Last Update</small><b>{date}</b></div><button className="secondary-button application-details">View Details</button><div className="application-menu-wrap"><button className="application-more" aria-label={`More options for ${title}`} aria-expanded={openMenu === title} onClick={() => setOpenMenu(openMenu === title ? null : title)}>⋮</button>{openMenu === title && <div className="application-menu"><button onClick={() => setOpenMenu(null)}>View application</button><button onClick={() => setOpenMenu(null)}>Download details</button><button className="danger" onClick={() => setOpenMenu(null)}>Withdraw application</button></div>}</div></article>)}</div><div className="application-pagination">Showing 1 to {visible.length} of 8 applications <span><button>‹</button><button className="active">1</button><button>2</button><button>›</button></span></div></div><aside className="application-aside"><div className="application-summary panel"><h2>Application Summary</h2><div className="summary-donut"><span>8<small>Total</small></span></div><ul><li className="green-dot">Selected <b>2 (25%)</b></li><li className="orange-dot">In Review <b>2 (25%)</b></li><li className="purple-dot">Shortlisted <b>2 (25%)</b></li><li className="pink-dot">Not Selected <b>2 (25%)</b></li></ul></div><div className="quick-links panel"><h2>Quick Links</h2>{[['Explore Opportunities', 'Find and filter opportunities', Rocket], ['My Requests', 'View your submitted requests', FileText], ['Upload Documents', 'Add your certificates & resumes', FileText], ['My Profile', 'Update your profile details', UserRound]].map(([title, note, Icon]) => <button key={title as string} onClick={() => title === 'My Requests' ? window.history.pushState({}, '', '/requests') : undefined}><Icon /><span><strong>{title}</strong><small>{note}</small></span><ChevronRight /></button>)}</div></aside></div></div> }
function ListView({ title, subtitle, action, rows, type }: any) { const [tab, setTab] = useState('All'); const [extraRows, setExtraRows] = useState<string[][]>([]); const [open, setOpen] = useState(false); const [form, setForm] = useState({ title: '', type: type === 'request' ? 'Guidance' : 'Academic Project', description: '' }); const allRows = [...rows, ...extraRows]; const visible = tab === 'All' ? allRows : allRows.filter((row) => tab === 'Pending' ? ['Pending', 'Under Review'].includes(row[2]) : ['Approved', 'Done', 'Selected'].includes(row[2])); const submit = () => { if (!form.title.trim()) return; setExtraRows((current) => [...current, [form.title, form.type, 'Pending', 'Just now']]); setForm({ title: '', type: type === 'request' ? 'Guidance' : 'Academic Project', description: '' }); setOpen(false) }; return <><PageIntro title={title} subtitle={subtitle} action={action && <button className="primary-button" onClick={() => setOpen(true)}><Plus />{type === 'request' ? 'New Request' : 'New Project'}</button>} /><Panel title={title} action={<div className="tabs compact">{['All', 'Pending', 'Done'].map((item) => <button key={item} className={tab === item ? 'active' : ''} onClick={() => setTab(item)}>{item} ({item === 'All' ? allRows.length : visible.length})</button>)}</div>}><div className="data-list">{visible.map((row: string[]) => <div className="data-row" key={row[0]}><span className={`row-icon ${type === 'request' ? 'orange' : 'blue'}`}>{type === 'request' ? <ClipboardList /> : <FileText />}</span><div><strong>{row[0]}</strong><small>{row[1]}</small></div><Status text={row[2]} /><small>{row[3]}</small><button className="ghost-button" onClick={() => setOpen(true)}>View <ExternalLink /></button></div>)}</div></Panel>{open && <Modal title={type === 'request' ? 'Create a new request' : 'Create a new project'} onClose={() => setOpen(false)}><div className="modal-form"><label>{type === 'request' ? 'Request title' : 'Project name'}<input value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} placeholder={type === 'request' ? 'What do you need help with?' : 'Enter your project name'} /></label><label>{type === 'request' ? 'Request type' : 'Project type'}<select value={form.type} onChange={(e) => setForm({ ...form, type: e.target.value })}>{(type === 'request' ? ['Guidance', 'Mentorship', 'IPR Support', 'Project Review'] : ['Academic Project', 'Personal Project', 'Startup Idea', 'Research']).map((option) => <option key={option}>{option}</option>)}</select></label><label>Description<textarea value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} placeholder="Tell us more..." /></label><div className="modal-actions"><button className="secondary-button" onClick={() => setOpen(false)}>Cancel</button><button className="primary-button" onClick={submit}>{type === 'request' ? 'Submit Request' : 'Add Project'}</button></div></div></Modal>}</> }
function Status({ text }: { text: string }) { return <span className={`status ${text.toLowerCase().replaceAll(' ', '-')}`}>{text}</span> }
function Projects({ go }: { go: (view: View) => void }) { const [tab, setTab] = useState('All Projects'); const [search, setSearch] = useState(''); const [menu, setMenu] = useState<string | null>(null); const [projectsState, setProjectsState] = useState(projects); const filteredProjects = projectsState.filter((project: any) => `${project[0]} ${project[1]}`.toLowerCase().includes(search.toLowerCase()) && (tab === 'All Projects' || (tab === 'Active' && project[2] === 'Active') || (tab === 'Done' && project[2] === 'Done') || (tab === 'On Hold' && project[2] === 'On Hold'))); const addProject = () => { const name = window.prompt('Enter a project name'); if (name?.trim()) setProjectsState((items: any[]) => [[name.trim(), 'Personal Project', 'Active', 'Just now', 0, UsersRound, 'blue'], ...items]); }; return <div className="projects-page"><PageIntro title="My Projects" subtitle="Track your projects, milestones and progress." action={<button className="primary-button" onClick={addProject}><Plus /> New Project / Join Project</button>} /><div className="project-stat-grid"><div className="project-stat blue"><BriefcaseBusiness /><strong>2</strong><span>Active Projects</span></div><div className="project-stat green"><CheckCircle2 /><strong>1</strong><span>Done</span></div><div className="project-stat purple"><Clock3 /><strong>1</strong><span>Pending / On Hold</span></div><div className="project-stat orange"><UsersRound /><strong>1</strong><span>Collaborating</span></div></div><div className="projects-layout"><div className="projects-main"><div className="project-toolbar"><div className="project-tabs">{['All Projects', 'Active', 'Done', 'On Hold'].map((item) => <button className={tab === item ? 'active' : ''} key={item} onClick={() => setTab(item)}>{item}</button>)}</div><label className="project-search"><Search /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search your projects..." /></label></div><div className="project-cards">{filteredProjects.map(([title, type, status, date, progress, Icon, tone]: any) => <article className="project-card" key={title}><span className={`project-icon ${tone || 'blue'}`}>{(() => { const ProjectIcon = Icon || BriefcaseBusiness; return <ProjectIcon /> })()}</span><div className="project-content"><div className="project-title-row"><strong>{title}</strong><span className={`project-tag ${tone}`}>{type}</span><span className={`project-status ${status.toLowerCase().replace(' ', '-')}`}>{status}</span></div><p>{title === 'AI-Based Study Planner' ? 'A personalized study planner that uses AI to recommend study schedules based on learning patterns.' : 'Research and development project focused on creating meaningful impact through technology.'}</p><small><UsersRound /> {status === 'Done' ? 'Individual Project' : 'Team Project (4 members)'} <CalendarDays /> Started: {date} <Flag /> {status === 'Done' ? 'Done: Aug 01, 2026' : 'Due: Nov 30, 2026'}</small></div><div className="project-progress"><div><span style={{ width: `${progress}%` }} /></div><b>{progress}%</b><button className="secondary-button" onClick={() => window.alert(`${title} details opened`)}>View Details</button></div><div className="project-menu-wrap"><button className="application-more" aria-label={`More options for ${title}`} onClick={() => setMenu(menu === title ? null : title)}>⋮</button>{menu === title && <div className="application-menu"><button onClick={() => window.alert(`Editing ${title}`)}>Edit project</button><button onClick={() => window.alert(`Milestones for ${title}`)}>View milestones</button><button className="danger" onClick={() => setProjectsState((items: any[]) => items.filter((item) => item[0] !== title))}>Remove project</button></div>}</div></article>)}</div></div><aside className="projects-aside"><div className="project-progress-panel panel"><h2>Overall Project Progress</h2><div className="project-donut"><span>66%<small>Overall</small></span></div><ul><li className="green-dot">Done <b>1</b></li><li className="blue-dot">In Progress <b>2</b></li><li className="orange-dot">On Hold <b>1</b></li><li className="gray-dot">Not Started <b>0</b></li></ul></div><div className="project-categories panel"><h2>Project Categories</h2><button onClick={() => setTab('All Projects')}><BriefcaseBusiness /> Major Project <b>1</b></button><button onClick={() => setTab('All Projects')}><Search /> Research <b>1</b></button><button onClick={() => setTab('All Projects')}><Lightbulb /> Innovation <b>1</b></button><button onClick={() => setTab('All Projects')}><BriefcaseBusiness /> Internship <b>1</b></button></div><div className="quick-links panel"><h2>Quick Links</h2><button onClick={() => go('opportunities')}><Search /><span><strong>Find Project Opportunities</strong><small>Search project opportunities</small></span><ChevronRight /></button><button onClick={() => go('mentor')}><UsersRound /><span><strong>Request a Mentor</strong><small>Connect with a mentor</small></span><ChevronRight /></button><button onClick={() => window.alert('Project guidelines opened')}><FileText /><span><strong>Project Guidelines</strong><small>Review requirements</small></span><ChevronRight /></button><button onClick={() => window.alert('Report submission opened')}><FileText /><span><strong>Submit Project Report</strong><small>Upload your report</small></span><ChevronRight /></button></div><div className="project-help panel"><Lightbulb /><div><h2>Need Help with Your Projects?</h2><p>Reach out to your mentor or the support team for assistance.</p><button className="secondary-button" onClick={() => window.alert('Support request opened')}>Contact Support</button></div></div></aside></div></div> }
function Mentor() { return <><PageIntro title="My Mentor" subtitle="Connect and learn with your mentor." /><div className="mentor-grid"><Panel title="Your mentor"><div className="mentor-card"><div className="mentor-avatar">AK</div><div><h2>Dr. Ayesha Khan</h2><p>Faculty Mentor<br />AI & Machine Learning</p><div className="mentor-actions"><button className="primary-button"><Mail /> Email</button><button className="secondary-button"><CalendarDays /> Schedule Meeting</button></div></div></div></Panel><Panel title="Mentorship progress"><div className="mentor-progress"><div className="progress-ring small"><span>80%</span></div><div><strong>3 of 5 milestones completed</strong><div className="bar"><span style={{ width: '60%' }} /></div></div></div></Panel></div><Panel title="Upcoming meetings"><EventRows /></Panel></> }
function MentorDashboard() { const [tab, setTab] = useState('Overview'); const [tasks, setTasks] = useState([false, false, false]); const [meeting, setMeeting] = useState(false); const [message, setMessage] = useState(false); return <div className="mentor-page"><PageIntro title="My Mentor" subtitle="Connect, learn and grow with your mentor" action={<button className="primary-button" onClick={() => setMessage(true)}><Plus /> Request a New Mentor</button>} /><div className="mentor-top-grid"><section className="mentor-profile-card panel"><div className="mentor-profile-main"><div className="mentor-photo">AK</div><div><h2>Dr. Arjun Mehta</h2><p>Associate Professor, Computer Science</p><small>SUKHF Mentor</small><span className="mentor-active"><CheckCircle2 /> Active Mentor</span></div><div className="mentor-actions"><button className="secondary-button" onClick={() => setMessage(true)}><Mail /> Message</button><button className="primary-button" onClick={() => setMeeting(true)}><CalendarDays /> Schedule Meeting</button></div></div><div className="mentor-contact"><span><Mail /> arjun.mehta@sukhf.org</span><span><GraduationCap /> School of Technology</span><span><MapPin /> Hyderabad, India</span></div></section><section className="mentor-focus panel"><h2>Mentorship Focus Areas</h2><div>{['AI & Machine Learning','Research Guidance','Project Development','Career Guidance','Startup & Innovation','Technical Writing'].map((item, index) => <button key={item} className={`focus-pill focus-${index}`} onClick={() => setTab('Overview')}>{item}</button>)}</div></section></div><div className="mentor-tabs">{['Overview','Meetings','Tasks','Milestones','Resources','Notes'].map((item) => <button key={item} className={tab === item ? 'active' : ''} onClick={() => setTab(item)}>{item}</button>)}</div>{tab !== 'Overview' && <section className="panel mentor-tab-panel"><div className="section-heading"><h2>{tab}</h2><button onClick={() => setTab('Overview')}>Back to Overview <ChevronRight /></button></div><div className="mentor-tab-content">{tab === 'Meetings' && <><strong>Upcoming and recent meetings</strong><p>Mentorship Meeting · Oct 10, 2026 · 04:00 PM – 05:00 PM</p><button className="primary-button" onClick={() => setMeeting(true)}>Schedule a Meeting</button></>}{tab === 'Tasks' && <><strong>Tasks from Dr. Arjun Mehta</strong><p>{tasks.filter(Boolean).length} of {tasks.length} tasks completed.</p>{tasks.map((done, index) => <label key={index}><input type="checkbox" checked={done} onChange={() => setTasks((current) => current.map((value, taskIndex) => taskIndex === index ? !value : value))} /> {['Prepare project milestone report','Read and summarize research papers','Update prototype and share demo'][index]}</label>)}</>}{tab === 'Milestones' && <><strong>Project milestones</strong><p>3 of 5 milestones completed for AI-Based Study Planner.</p><div className="bar"><span style={{ width: '60%' }} /></div></>}{tab === 'Resources' && <><strong>Mentor resources</strong><p>Research methodology, AI tools, and project report templates are ready to explore.</p><button className="secondary-button" onClick={() => window.alert('Resources opened')}>Browse Resources</button></>}{tab === 'Notes' && <><strong>Mentorship notes</strong><textarea className="mentor-notes" placeholder="Add a note for your next meeting..." /><button className="primary-button" onClick={() => window.alert('Note saved')}>Save Note</button></> }</div></section>}<div className="mentor-dashboard-grid"><div className="mentor-left"><section className="panel engagement-card"><div className="section-heading"><h2>Current Engagement</h2><button onClick={() => setTab('Milestones')}>View Project <ChevronRight /></button></div><div className="engagement-body"><span className="engagement-icon"><Sparkles /></span><div><strong>AI-Based Study Planner</strong><span className="project-tag purple">Major Project</span><p>A personalized study planner that uses AI to recommend study schedules based on learning patterns.</p><div className="mentor-progress"><span>Progress</span><div><i /></div><b>70%</b></div></div></div></section><section className="panel mentor-list-card"><div className="section-heading"><h2>Recent Meetings</h2><button onClick={() => setTab('Meetings')}>View All</button></div>{['Project Progress Discussion','Research Paper Ideas','Career Guidance Session'].map((item, index) => <button className="mentor-list-row" key={item} onClick={() => setMeeting(true)}><span className={`list-icon list-${index}`}><UsersRound /></span><strong>{item}</strong><small>{['Sep 25, 2026','Sep 10, 2026','Aug 28, 2026'][index]}</small><ChevronRight /></button>)}</section><section className="panel mentor-list-card"><div className="section-heading"><h2>Mentor&apos;s Resources</h2><button onClick={() => setTab('Resources')}>View All</button></div>{['Guide to Research Methodology','AI Tools for Students','Project Report Template'].map((item, index) => <button className="mentor-list-row" key={item} onClick={() => window.alert(`${item} opened`)}><span className="list-icon list-blue"><FileText /></span><span><strong>{item}</strong><small>{['PDF · 2.4 MB','Link','DOCX · 1.1 MB'][index]}</small></span><ChevronRight /></button>)}</section></div><div className="mentor-right"><section className="panel upcoming-meeting"><div className="section-heading"><h2>Upcoming Meeting</h2><span className="confirmed"><CheckCircle2 /> Confirmed</span></div><div className="meeting-title"><span className="list-icon list-blue"><CalendarDays /></span><div><strong>Mentorship Meeting</strong><p><CalendarDays /> Oct 10, 2026</p><p><Clock3 /> 04:00 PM – 05:00 PM</p><p><Link2 /> Google Meet</p></div></div><div className="meeting-actions"><button className="secondary-button" onClick={() => setMeeting(true)}>Reschedule</button><button className="primary-button" onClick={() => window.alert('Joining Google Meet')}>Join Meeting</button></div></section><section className="panel mentor-list-card"><div className="section-heading"><h2>Tasks from Mentor</h2><button onClick={() => setTab('Tasks')}>View All</button></div>{['Prepare project milestone report','Read and summarize research papers','Update prototype and share demo'].map((item, index) => <label className="mentor-task" key={item}><input type="checkbox" checked={tasks[index]} onChange={() => setTasks((current) => current.map((value, taskIndex) => taskIndex === index ? !value : value))} /><strong>{item}</strong><small>Due: {['Oct 05, 2026','Oct 12, 2026','Oct 20, 2026'][index]}</small><span className={`task-status task-${index}`}>{tasks[index] ? 'Done' : ['In Progress','Pending','Not Started'][index]}</span></label>)}</section><section className="mentor-guidance panel"><Sparkles /><div><h2>Make the Most of Your Mentorship</h2><p>Come prepared, be proactive, and keep track of your progress.<br />Good mentorship can open new opportunities!</p></div></section></div></div>{(message || meeting) && <Modal title={message ? 'Message your mentor' : 'Schedule a meeting'} onClose={() => { setMessage(false); setMeeting(false) }}><p>{message ? 'Your mentor will receive a message request.' : 'Choose a meeting time with Dr. Arjun Mehta.'}</p><button className="primary-button" onClick={() => { setMessage(false); setMeeting(false); window.alert(message ? 'Message sent' : 'Meeting request sent') }}>{message ? 'Send Message' : 'Request Meeting'}</button></Modal>}</div> }

function Events() { const [tab, setTab] = useState('Upcoming Events'); const [category, setCategory] = useState('All Categories'); const [query, setQuery] = useState(''); const [registered, setRegistered] = useState<string[]>(['AI for Social Impact Workshop']); const [notificationsEnabled, setNotificationsEnabled] = useState(false); const [month, setMonth] = useState('October 2026'); const categories = [['Workshops', CalendarDays], ['Webinars', Monitor], ['Competitions', Trophy], ['Seminars', UsersRound], ['Networking', Share2], ['Training Programs', GraduationCap]] as const; const events = [{ title: 'AI for Social Impact Workshop', type: 'Workshop', date: 'Oct 15, 2026', time: '10:00 AM – 1:00 PM', place: 'SUKHF Campus', spots: '120 spots', image: 'AI for a Better Tomorrow', tone: 'blue' }, { title: 'AI Tools for Student Success', type: 'Webinar', date: 'Oct 18, 2026', time: '5:00 PM – 6:00 PM', place: 'Online', spots: '500 spots', image: 'AI Student Webinar', tone: 'pink' }, { title: 'SUKHF Networking Connect', type: 'Networking', date: 'Oct 25, 2026', time: '11:00 AM – 1:00 PM', place: 'SUKHF Campus', spots: '80 spots', image: 'Networking Connect', tone: 'green' }, { title: 'Sustainable Innovation for a Greener Future', type: 'Seminar', date: 'Oct 22, 2026', time: '2:00 PM – 4:00 PM', place: 'Online', spots: '200 spots', image: 'Sustainable Future', tone: 'green' }, { title: 'SUKHF Innovation Challenge 2026', type: 'Competition', date: 'Nov 05, 2026', time: '9:00 AM – 5:00 PM', place: 'SUKHF Auditorium', spots: '100 spots', image: 'Innovation Challenge', tone: 'orange' }, { title: 'Full Stack Development Bootcamp', type: 'Training', date: 'Nov 15, 2026', time: '10:00 AM – 4:00 PM', place: 'SUKHF Lab', spots: '50 spots', image: 'Full Stack Bootcamp', tone: 'pink' }]; const visible = events.filter((event) => (category === 'All Categories' || (category === 'Workshops' && event.type === 'Workshop') || (category === 'Webinars' && event.type === 'Webinar') || (category === 'Competitions' && event.type === 'Competition') || (category === 'Seminars' && event.type === 'Seminar') || (category === 'Networking' && event.type === 'Networking') || (category === 'Training Programs' && event.type === 'Training')) && event.title.toLowerCase().includes(query.toLowerCase())); const toggleRegistration = (title: string) => setRegistered((current) => current.includes(title) ? current.filter((item) => item !== title) : [...current, title]); return <div className="events-page"><PageIntro title="Events & Activities" subtitle="Discover and participate in workshops, webinars, competitions and more." action={<button className="primary-button" onClick={() => { setTab('Upcoming Events'); setCategory('All Categories'); setQuery(''); }}><Plus /> Browse All Events</button>} /><div className="event-category-grid">{categories.map(([label, Icon], index) => <button key={label} className={`event-category event-category-${index}`} onClick={() => setCategory(label)}><Icon /><span>{label}</span></button>)}</div><div className="events-tabs"><div>{['Upcoming Events', 'Past Events', 'My Registrations'].map((item) => <button key={item} className={tab === item ? 'active' : ''} onClick={() => setTab(item)}>{item}</button>)}</div><select value={category} onChange={(event) => setCategory(event.target.value)}><option>All Categories</option>{categories.map(([label]) => <option key={label}>{label}</option>)}</select><label className="event-search"><Search /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search events..." /></label></div><div className="events-layout"><section className="event-results panel"><div className="section-heading"><h2>{tab}</h2><span>{visible.length} events</span></div>{(tab === 'My Registrations' ? events.filter((event) => registered.includes(event.title)) : visible).map((event) => <article className="event-card" key={event.title}><div className={`event-image ${event.tone}`}><strong>{event.image}</strong></div><div className="event-card-content"><span className={`event-type ${event.tone}`}>{event.type}</span><h3>{event.title}</h3><p>Learn how ideas, technology and collaboration can create meaningful real-world impact.</p><small><CalendarDays />{event.date}<Clock3 />{event.time}<MapPin />{event.place}<UsersRound />{event.spots}</small></div><div className="event-card-action"><span className="registration-open">Registration Open</span><button className={registered.includes(event.title) ? 'secondary-button' : 'primary-button'} onClick={() => toggleRegistration(event.title)}>{registered.includes(event.title) ? 'Registered' : 'Register'}</button></div></article>)}</section><aside className="events-sidebar"><section className="panel event-calendar"><div className="section-heading"><h2>Event Calendar</h2><button onClick={() => setMonth(month === 'October 2026' ? 'November 2026' : 'October 2026')}>View Full Calendar</button></div><div className="calendar-heading"><button onClick={() => setMonth('September 2026')}><ChevronLeft /></button><strong>{month}</strong><button onClick={() => setMonth('November 2026')}><ChevronRight /></button></div><div className="calendar-days">{['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].map((day) => <b key={day}>{day}</b>)}{Array.from({ length: 35 }, (_, index) => <button key={index} className={[6,15,22,30].includes(index) ? 'marked' : ''}>{index < 4 ? '' : ((index - 3) % 30) || 30}</button>)}</div></section><section className="panel registrations-card"><div className="section-heading"><h2>My Registrations</h2><button onClick={() => setTab('My Registrations')}>View All</button></div>{events.slice(0, 3).map((event) => <button className="registration-row" key={event.title} onClick={() => toggleRegistration(event.title)}><span className={`registration-icon ${event.tone}`}><CalendarDays /></span><span><strong>{event.title}</strong><small>{event.date}</small></span><Status text={registered.includes(event.title) ? 'Registered' : 'Pending'} /></button>)}</section><section className="event-notify panel"><CalendarDays /><div><h2>Stay Updated</h2><p>Get notified about new events and activities.</p><button className="secondary-button" onClick={() => setNotificationsEnabled(!notificationsEnabled)}>{notificationsEnabled ? 'Notifications Enabled' : 'Enable Notifications'}</button></div></section></aside></div></div> }
function EventRows({ compact = false, registered = [], setRegistered = () => {}, past = false }: { compact?: boolean; registered?: string[]; setRegistered?: (value: string[] | ((current: string[]) => string[])) => void; past?: boolean }) { const items = [['SEP', '28', 'Innovation & Startup Talk', '10:00 AM – 12:00 PM', 'Online'], ['OCT', '05', 'Drone Technology Workshop', '10:00 AM – 02:00 PM', 'Hyderabad'], ['OCT', '12', 'IPR Awareness Session', '02:00 PM – 04:00 PM', 'Online']]; return <div className="event-list">{items.slice(0, compact ? 2 : 3).map((item) => <div className="event" key={item[2]}><time><b>{item[0]}</b><strong>{item[1]}</strong></time><div><strong>{item[2]}</strong><span>SUKHF</span><small><CalendarDays />{item[3]} <MapPin />{item[4]}</small></div>{past ? <Status text="Done" /> : <button className="ghost-button" onClick={() => setRegistered((current) => registered.includes(item[2]) ? current.filter((name) => name !== item[2]) : [...current, item[2]])}>{compact ? <ChevronRight /> : registered.includes(item[2]) ? 'Registered' : 'Register'}</button>}</div>)}</div> }
function AnnouncementRows() { return <div>{['Applications Open for IGNITE 2026', 'New Research Opportunities Added', 'Mentorship Program Applications Live'].map((item, index) => <div className="announcement" key={item}><span className="announcement-icon"><Bell /></span><strong>{item}</strong><small>Sep {20 - index * 2}, 2026</small></div>)}</div> }
function Notifications({ unread, setUnread }: any) { const [read, setRead] = useState<string[]>([]); const [tab, setTab] = useState('All'); const [category, setCategory] = useState('All Categories'); const [dateRange, setDateRange] = useState('All Time'); const [preferences, setPreferences] = useState([true, true, true, true, true]); const tabs = ['All', 'Unread', 'Messages', 'Opportunities', 'Events', 'Mentorship', 'System']; const tabTypes: Record<string, string> = { Messages: 'message', Opportunities: 'opportunity', Events: 'event', Mentorship: 'mentor', System: 'system' }; const iconFor = (type: string) => type === 'event' ? CalendarDays : type === 'mentor' ? UsersRound : type === 'message' ? MessageCircle : type === 'opportunity' ? FileText : type === 'system' ? Settings : Bell; const typed = notifications.map((item: any, index: number) => [...item, ['opportunity', 'event', 'mentor', 'opportunity', 'event', 'message', 'system', 'event'][index]]); const filtered = typed.filter((item: any) => { const matchesTab = tab === 'All' || (tab === 'Unread' && !read.includes(item[0])) || tabTypes[tab] === item[4]; const matchesCategory = category === 'All Categories' || category.toLowerCase().startsWith(item[4]); return matchesTab && matchesCategory; }); const toggleRead = (title: string) => { const next = read.includes(title) ? read.filter((item) => item !== title) : [...read, title]; setRead(next); setUnread(Math.max(0, notifications.length - next.length)); }; const markAll = () => { setRead(notifications.map((item: any) => item[0])); setUnread(0); }; return <div className="notifications-page"><PageIntro title="Notifications" subtitle="Stay updated with important updates, deadlines and messages." action={<button className="secondary-button" onClick={markAll}><Check /> Mark All as Read</button>} /><div className="notification-summary"><div className="notification-summary-card blue"><span className="notification-summary-icon"><Bell /></span><div><strong>{Math.max(0, notifications.length - read.length)}</strong><small>Unread</small></div></div><div className="notification-summary-card green"><span className="notification-summary-icon"><Mail /></span><div><strong>12</strong><small>Total Notifications</small></div></div><div className="notification-summary-card purple"><span className="notification-summary-icon"><CalendarDays /></span><div><strong>4</strong><small>Event Updates</small></div></div><div className="notification-summary-card orange"><span className="notification-summary-icon"><Send /></span><div><strong>5</strong><small>Application Updates</small></div></div></div><div className="notifications-layout"><main><div className="notification-filters">{tabs.map((item) => <button key={item} className={tab === item ? 'active' : ''} onClick={() => setTab(item)}>{item}{item === 'Unread' ? ` (${Math.max(0, notifications.length - read.length)})` : ''}</button>)}</div><section className="panel notification-list-panel"><div className="notification-list">{filtered.map((item: any) => { const Icon = iconFor(item[4]); const isRead = read.includes(item[0]); return <button className={`notification-row ${isRead ? 'read' : 'unread'} ${item[3]}`} key={item[0]} onClick={() => toggleRead(item[0])}><span className="notification-icon"><Icon /></span><span className="notification-copy"><strong>{item[0]}</strong><p>{item[1]}</p></span><small>{item[2]}</small>{!isRead && <i className="notification-dot" />}<ChevronRight /></button>})}</div>{filtered.length === 0 && <p className="empty-state">No notifications match these filters.</p>}</section></main><aside className="notification-rail"><section className="panel"><div className="section-heading"><h2>Notification Preferences</h2></div><p className="rail-description">Choose what you want to be notified about.</p>{['Opportunities','Applications','Mentorship','Events & Activities','Messages'].map((label, index) => <label className="notification-toggle" key={label}><span>{label}</span><input type="checkbox" checked={preferences[index]} onChange={() => setPreferences((current) => current.map((value, i) => i === index ? !value : value))} /></label>)}</section><section className="panel"><div className="section-heading"><h2>Filter Notifications</h2></div><label className="notification-filter-field">Category<select value={category} onChange={(event) => setCategory(event.target.value)}><option>All Categories</option><option>Opportunity</option><option>Event</option><option>Mentor</option><option>Message</option><option>System</option></select></label><label className="notification-filter-field">Date Range<select value={dateRange} onChange={(event) => setDateRange(event.target.value)}><option>All Time</option><option>Today</option><option>This Week</option><option>This Month</option></select></label></section><section className="panel notification-support"><h2>Need Help?</h2><p>If you&apos;re not receiving notifications or facing any issues, reach out to our support team.</p><button className="secondary-button" onClick={() => window.alert('Support request opened')}>Contact Support</button></section></aside></div></div> }
function SettingsView() { const [tab, setTab] = useState('Account'); const [toggles, setToggles] = useState([true, true, true, true, true]); const [theme, setTheme] = useState('Light'); const [language, setLanguage] = useState('English (Default)'); const tabs = [['Account', UserRound], ['Notifications', Bell], ['Privacy & Security', ShieldCheck], ['Appearance', Sparkles], ['Language', Globe2], ['Help & Support', Headphones]]; return <div className={`settings-page theme-${theme.toLowerCase()}`}><PageIntro title="Settings" subtitle="Manage your account, preferences and privacy settings." /><div className="settings-tabs">{tabs.map(([label, Icon]) => <button key={label as string} className={tab === label ? 'active' : ''} onClick={() => setTab(label as string)}><Icon />{label as string}</button>)}</div>{tab !== 'Account' && <section className="panel settings-tab-panel"><h2>{tab}</h2><p>{tab === 'Notifications' ? 'Control opportunities, applications, mentorship, events and system alerts.' : tab === 'Privacy & Security' ? 'Manage password protection and account security preferences.' : tab === 'Appearance' ? 'Choose Light, Dark, or System colors for the portal.' : tab === 'Language' ? 'Select the language used throughout your portal.' : 'Find answers and contact the SUKHF support team.'}</p>{tab === 'Notifications' && ['Opportunities','Applications','Mentorship','Events & Activities','System Updates'].map((label,index) => <label className="settings-toggle-row" key={label}><span>{label}<small>Updates and reminders</small></span><button className={`toggle ${toggles[index] ? 'on' : ''}`} onClick={() => setToggles((current) => current.map((value,i) => i === index ? !value : value))}><i /></button></label>)}{tab === 'Privacy & Security' && <div className="settings-tab-actions"><button className="secondary-button" onClick={() => window.alert('Password update opened')}><LockKeyhole /> Update Password</button><button className="secondary-button" onClick={() => window.alert('Two-factor authentication setup opened')}><ShieldCheck /> Set Up Two-Factor Authentication</button></div>}{tab === 'Appearance' && <div className="theme-options">{['Light','Dark','System'].map((item) => <button key={item} className={theme === item ? 'selected' : ''} onClick={() => setTheme(item)}>{item}</button>)}</div>}{tab === 'Language' && <select className="settings-tab-select" value={language} onChange={(event) => setLanguage(event.target.value)}><option>English (Default)</option><option>Hindi</option><option>Telugu</option></select>}{tab === 'Help & Support' && <div className="settings-tab-actions"><button className="primary-button" onClick={() => window.alert('Support request opened')}><Headphones /> Contact Support</button><button className="secondary-button" onClick={() => window.alert('Help center opened')}>Open Help Center</button></div>}</section>}<div className={`settings-dashboard ${tab !== 'Account' ? 'settings-dashboard-hidden' : ''}`}><div className="settings-main"><section className="panel profile-settings-card"><div className="settings-card-heading"><div><h2><span className="settings-heading-icon"><UserRound /></span>Profile Information</h2><p>Update your personal information and profile details.</p></div><button className="secondary-button"><Pencil /> Edit Profile</button></div><div className="profile-identity"><div className="profile-avatar-large">A</div><div><h3>Asma</h3><p>Student</p><small>asma@example.com</small><span className="profile-completed"><CheckCircle2 /> Profile Done</span></div></div><div className="profile-details">{[['Full Name','Asma'],['Email Address','asma@example.com'],['Phone Number','+91 98765 43210'],['Date of Birth','May 14, 2004'],['Institution','SUKHF / University Name'],['Course','B.Sc. Computer Science'],['Year','3rd Year'],['Location','Hyderabad, India']].map(([label,value]) => <div key={label}><strong>{label}</strong><span>{value}</span></div>)}</div></section><section className="panel password-settings-card"><div className="settings-card-heading"><div><h2><span className="settings-heading-icon"><LockKeyhole /></span>Change Password</h2><p>Keep your account secure with a strong password.</p></div><button className="secondary-button">Update Password</button></div><div className="password-grid"><div>{['Current Password','New Password','Confirm New Password'].map((label) => <label key={label}>{label}<span><input type="password" placeholder={`Enter ${label.toLowerCase()}`} /><Eye /></span></label>)}</div><div className="password-tips"><strong><ShieldCheck /> Password Tips</strong><p>Use at least 8 characters</p><p>Include uppercase and lowercase letters</p><p>Include a number</p><p>Include a special character</p></div></div></section></div><aside className="settings-sidebar"><section className="panel settings-side-card"><h2><Bell /> Notification Preferences</h2><p>Choose what you want to be notified about.</p>{['Opportunities','Applications','Mentorship','Events & Activities','System Updates'].map((label,index) => <label className="settings-toggle-row" key={label}><span>{label}<small>Updates and reminders</small></span><button className={`toggle ${toggles[index] ? 'on' : ''}`} onClick={() => setToggles((current) => current.map((value,i) => i === index ? !value : value))}><i /></button></label>)}</section><section className="panel settings-side-card"><h2><Sparkles /> Appearance</h2><p>Customize how the platform looks for you.</p><div className="theme-options">{['Light','Dark','System'].map((item) => <button key={item} className={theme === item ? 'selected' : ''} onClick={() => setTheme(item)}>{item}</button>)}</div></section><section className="panel settings-side-card"><h2><Globe2 /> Language</h2><p>Select your preferred language.</p><select value={language} onChange={(event) => setLanguage(event.target.value)}><option>English (Default)</option><option>Hindi</option><option>Telugu</option></select></section><section className="panel delete-settings-card"><h2>Delete Account</h2><p>Permanently delete your account and all your data.</p><button className="secondary-button">Delete Account</button></section></aside></div></div> }
function Journey() { return <><div className="journey-track">{['Profile Done', 'Applied', 'Selected', 'In Progress', 'Evaluation', 'Done'].map((step, index) => <div className={`journey-step ${index < 2 ? 'done' : index === 2 ? 'current' : ''}`} key={step}><span>{index < 2 ? <Check /> : ''}</span><small>{step}</small></div>)}</div><div className="journey-banner"><img className="journey-image" src="/sukhf-journey.png" alt="Student building an innovation project" /><div><strong>You are on your way!</strong><p>Keep exploring, applying and learning.<br />Your journey with SUKHF has just begun.</p></div></div></> }
